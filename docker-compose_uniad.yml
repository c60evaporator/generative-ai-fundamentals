services:
  uniad-dev:
    image: "${IMAGE_NAME:-uniad-dev}:${IMAGE_TAG:-uniad2.0}"
    # docker build options
    build:
      context: .
      dockerfile: docker/Dockerfile_uniad
      # --build-arg option
      args:
        # CUDA and Ubuntu Versions
        CUDA_VERSION: "${CUDA_VERSION:-11.8.0}"
        UBUNTU_VERSION: "${UBUNTU_VERSION:-22.04}"
        # Python version
        PYTHON_VERSION: "${PYTHON_VERSION:-3.9}"
        # PyTorch versions
        TORCH_CUDA: "${TORCH_CUDA:-cu118}"
        TORCH_VERSION: "${TORCH_VERSION:-2.0.1}"
        # OpenMMLab versions
        MINIFORGE_VERSION: "${MINIFORGE_VERSION:-24.11.0-1}"
        CONDA_DIR: "${CONDA_DIR:-/opt/conda}"
        # Match host UID/GID to avoid permission issues on bind mounts
        USERNAME: "${USERNAME:-developer}"
        UID: "${UID:-1000}"
        GID: "${GID:-1000}"

    # Container name (--name option)
    container_name: uniad_dev
    # Set resource sharing to host
    gpus: all  # --gpus
    ipc: host  # --ipc
    shm_size: "${SHM_SIZE:-16g}"  # --shm-size
    # Stable operation for development
    init: true  # --init option
    stdin_open: true  # -i option
    tty: true  # -t option
    # Set startup directory
    working_dir: /workspace

    # Environment variables (-e option)
    environment:
      # Base path for datasets inside the container
      DATASET_ROOT: "/workspace/data"
    
    # Volume mounts
    volumes:
      # Project (str, settings etc. managed by Git)
      - .:/workspace
